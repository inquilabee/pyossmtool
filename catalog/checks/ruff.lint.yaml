id: ruff.lint
tool: ruff
name: Ruff Lint
description: Run ruff check on Python sources.
target_key: python
argv:
  - check
  - "{target}"
  - --output-format
  - json
parser: ruff_json
success:
  exit_codes: [0]
remediation:
  suggested_commands:
    - "ruff check --fix {target}"
