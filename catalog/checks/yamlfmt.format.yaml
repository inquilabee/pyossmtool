id: yamlfmt.format
tool: yamlfmt
name: yamlfmt Lint
description: Lint YAML files for formatting issues.
target_key: yaml
argv:
  - -lint
  - "{target}"
parser: yamlfmt_text
success:
  exit_codes: [0]
remediation:
  suggested_commands:
    - "yamlfmt {target}"
