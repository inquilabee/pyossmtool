id: formatting
name: Formatting
description: All format-only checks across Python, shell, Markdown, and YAML.
env: auto
target: .
checks:
  - id: ruff.format
  - id: shfmt.format
  - id: mdformat.format
  - id: yamlfmt.format
