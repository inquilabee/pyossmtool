id: shfmt.format
tool: shfmt
name: shfmt Format Check
description: Verify shell script formatting via diff mode.
argv:
  - -d
  - '{target}'
parser: shfmt_diff
success:
  exit_codes:
    - 0
remediation:
  suggested_commands:
    - shipgate format --check shfmt.apply --target {target}
